# rm-pad config (TOML). Copy to rm-pad.toml or ~/.config/rm-pad.toml

host = "10.11.99.1"
# key_path = "~/.ssh/id_rsa"
# password = "your-root-password"

# touch_only = false
# pen_only = false
# grab_input = true   # on by default; set false to let tablet UI also see input
# no_palm_rejection = false
# palm_grace_ms = 500
# orientation = "landscape-right"
# tilt_correction = "off"       # "off", "tilt", or "tilt-distance"
# tilt_correction_gain = 1.0    # effective lever length in pen digitizer units (negative flips direction)
