# rm-pad config (TOML). Copy to rm-pad.toml or ~/.config/rm-pad.toml

host = "10.11.99.1"
# key_path = "~/.ssh/id_rsa"
# password = "your-root-password"

# touch_only = false
# pen_only = false
# grab_input = true   # on by default; set false to let tablet UI also see input
# no_palm_rejection = false
# palm_grace_ms = 500
# orientation = "landscape-right"
# fit = "fill"        # pen-to-screen mapping: "fill" (stretch, default),
                      # "contain" (keep aspect, letterboxed), or "cover"
                      # (keep aspect, edges cropped). Anything but "fill"
                      # requires "aspect_ratio" or "resolution".
